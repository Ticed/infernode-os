tool=hear
	description='Speech-to-text via /dev/audio and speech9p'
	authorities='reads_fs receives_input captures_audio'
	notes='Captures microphone audio and transcribes it. Requires /mnt/speech. captures_audio is a sensitive read of the room, not of the filesystem, so it is its own axis: nsconstruct refuses the grant unless the profile is egress-free.'
